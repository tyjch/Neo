# Record

```python
# Returns value in record by index
value = record[index]

# Returns value in record by key
value = record[key]

# Return number of fields in a record
len(record)

# Returns a dict representation of a record
dict(record)

# Returns a dict representation of contained keys and values
record.data()

# Returns a list of 2-tuples containing key-value pairs
record.items()

# Returns a tuple of names of keys
record.keys()

# Returns a tuple of contained values
record.values()

# Convert to a Subgraph if possible
record.subgraph()
```

